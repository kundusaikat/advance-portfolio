import { atom } from "recoil";

export const visiorCountState = atom({
  key: "visiorCountState",
  default: 0,
});

export const themeState = atom({
  key: 'themeState',
  default: 'default',
});

